---
- name: Set hostname
  hostname:
    name: '{{ hostname }}'

- name: Copy /etc/hosts with localhost definitions
  template:
    src: hosts.j2
    dest: /etc/hosts
    owner: root
    group: root
    mode: 0644
